<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-10 apply">
      <h3 class="title">
        現在申請中<br><br>
      </h3>

      <table class="table table-primary">
        <thead class="table-active">
        	<tr class="table-header">
        	  <th scope="col">ゲストハウス名</th>
        		<th>タイトル</th>
        		<th>募集内容</th>
        		<th>募集期間</th>
        		<th>ステータス</th>
        		<th></th>
        	</tr>
        </thead>

        <tbody>
          <% @guesthouse_applies.each do |apply| %>
            <tr>
              <td><%= apply.guesthouse_community.guesthouse.name %></td>
              <td data-label="タイトル"><%= apply.guesthouse_community.title %></td>
              <td data-label="募集内容"><%= simple_format(apply.guesthouse_community.body) %></td>
              <td data-label="募集期間">
                <% if apply.guesthouse_community.end_date < Date.today %>
                  募集終了しました
                <% else %>
                  <%= apply.guesthouse_community.start_date %> から <%= apply.guesthouse_community.end_date %>
                <% end %>
              </td>
              <td data-label="ステータス"><%= apply.status_i18n %></td>
              <td>
                <% if apply.status == "accept" %>
                  <%= link_to 'コミュニティに参加', guesthouse_community_guesthouse_community_comments_path(apply.guesthouse_community.id) %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>

    </div>
  </div>
  <div class="review-button">
  	<td><%= link_to "マイページへ戻る", customer_path(@customer.id), class: "btn btn btn-primary text-white" %></td>
  	</div>
  	</div>
  </div>
</section>
